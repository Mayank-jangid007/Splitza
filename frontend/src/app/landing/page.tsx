'use client'

import { useEffect, useRef, useState } from 'react'
import gsap from 'gsap'
import { AnimatePresence, motion } from 'motion/react'
import { ScrollToPlugin } from 'gsap/ScrollToPlugin'
import { ScrollTrigger } from 'gsap/ScrollTrigger'
import { ArrowRight, Check, ChevronDown, CircleDollarSign, LockKeyhole, Menu, Moon, ShieldCheck, Sparkles, Sun, Wallet, X } from 'lucide-react'
import SplitFlapLogoTicker from '@/components/split-flap-logo-ticker'
import './landing.css'

const marqueeRows = [
  ['NETFLIX', 'SPOTIFY', 'YOUTUBE', 'CHATGPT', 'CANVA', 'NOTION'],
  ['DISNEY+', 'ADOBE', 'FIGMA', 'DROPBOX', 'HULU', 'PRIME VIDEO'],
  ['APPLE TV+', 'CLAUDE', 'DUOLINGO', 'MICROSOFT 365', 'TIDAL', 'MUBI'],
]
const faqs = [
  ['Is sharing digital subscriptions legal?', 'SubSplit is designed around designated multi-user and family options. Always use plans according to each provider\u2019s terms.'],
  ['How does weekly payout protection work?', 'Host funds are released in disciplined 25% weekly installments after automated health checks confirm the plan is active.'],
  ['What if a host changes the password?', 'The payout freezes immediately, the group is notified, and eligible buyers can receive a prorated refund through the protection flow.'],
]
const hostSteps = ['LIST UNUSED SLOTS', 'BOT VERIFIES CREDENTIALS', 'AUTOMATED WEEKLY PAYOUTS']
const subSteps = ['SELECT A PREMIUM APP', 'CONNECT UPI AUTOPAY', 'GET ENCRYPTED ACCESS']

export default function Home() {
  const [dark, setDark] = useState(true)

  useEffect(() => {
    gsap.registerPlugin(ScrollToPlugin, ScrollTrigger)
    const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches
    const ctx = gsap.context(() => {
      if (!prefersReducedMotion) gsap.from('.neo-hero-copy > *, .neo-activation', { opacity: 0, y: 28, duration: 0.8, stagger: 0.08, ease: 'power3.out' })
      if (!prefersReducedMotion) { gsap.utils.toArray<HTMLElement>('.neo-workflow article, .neo-bento article, .neo-protection-grid > div, .neo-faq-list > div, .neo-engine-step').forEach((element) => { gsap.fromTo(element, { opacity: 0, y: 28 }, { opacity: 1, y: 0, duration: 0.7, ease: 'power3.out', scrollTrigger: { trigger: element, start: 'top 88%', once: true } }) }); ScrollTrigger.refresh() }
    })
    const journey = journeyRef.current
    const journeyTriggers = journey && !prefersReducedMotion ? Array.from(journey.querySelectorAll<HTMLElement>('.neo-engine-step')).map((step, index) => ScrollTrigger.create({ trigger: step, start: 'top 62%', end: 'bottom 38%', onEnter: () => setJourneyStep(index), onEnterBack: () => setJourneyStep(index) })) : []
    const links = Array.from(document.querySelectorAll<HTMLAnchorElement>('a[href^="#"]'))
    const handleClick = (event: MouseEvent) => {
      const link = event.currentTarget as HTMLAnchorElement
      const target = document.querySelector(link.hash)
      if (!target) return
      event.preventDefault()
      gsap.to(window, { duration: 0.9, scrollTo: { y: target, offsetY: 24 }, ease: 'power3.inOut' })
    }
    links.forEach((link) => link.addEventListener('click', handleClick))
    return () => { links.forEach((link) => link.removeEventListener('click', handleClick)); journeyTriggers.forEach((trigger) => trigger.kill()); ctx.revert() }
  }, [])
  const [menu, setMenu] = useState(false)
  const [journeyStep, setJourneyStep] = useState(0)
  const journeyRef = useRef<HTMLElement>(null)
  const [faq, setFaq] = useState<number[]>([])
  const [email, setEmail] = useState('')
  const [submitted, setSubmitted] = useState(false)

  return <main className={`neo-home ${dark ? 'neo-dark' : 'neo-light'}`}>
    <header className="neo-nav"><a href="#top" className="neo-logo"><span className="neo-logo-mark">S</span>SUBSPLIT</a><button className="neo-mobile-toggle" onClick={() => setMenu(!menu)} aria-label="Toggle navigation">{menu ? <X size={20} /> : <Menu size={20} />}</button><nav className={menu ? 'neo-links is-open' : 'neo-links'}><a href="#trust">ABOUT</a><a href="#faq">FAQ</a><a href="#how">GUIDE</a></nav><div className="neo-actions"><button className="neo-theme" onClick={() => { const updateTheme = () => setDark((value) => !value); if ('startViewTransition' in document) { (document as Document & { startViewTransition?: (callback: () => void) => void }).startViewTransition?.(updateTheme) } else { updateTheme() } }} aria-label="Toggle theme">{dark ? <Sun size={17} /> : <Moon size={17} />}</button><a href="/auth/login" className="neo-login">LOGIN</a><a href="/auth/register" className="neo-button neo-button-small">START SAVING <ArrowRight size={15} /></a></div></header>

    <section id="top" className="neo-hero neo-shell"><div className="neo-hero-copy"><span className="neo-chip"><Sparkles size={14} /> AUTOMATED & SECURE SUBSCRIPTION SPLITTING</span><h1>Share the cost.<br /><em>Not the risk.</em></h1><p>The automated escrow platform that instantly matches you into shared premium groups. Save up to 80% on Netflix, Spotify, and YouTube with auto-pay and real-time fraud protection.</p><div className="neo-hero-actions"><a href="/auth/register" className="neo-button">CREATE MY ACCOUNT <ArrowRight size={18} /></a></div></div><div className="neo-activation"><div className="neo-activation-card"><div className="neo-activation-head"><span className="neo-netflix">N</span><div><b>Netflix Premium 4K</b><small>Split active</small></div><span className="neo-status">ACTIVE</span></div><div className="neo-queue-label"><span>SMART QUEUE</span><b>3/4 VERIFIED</b></div><div className="neo-queue"><span className="neo-avatar neo-avatar-1">H</span><span className="neo-avatar">C1</span><span className="neo-avatar neo-avatar-2">C2</span><span className="neo-joining">+<small>Joining...</small></span></div><div className="neo-split-tile"><div><small>TOTAL PLAN COST</small><strong>&#8377;649/mo</strong></div><ArrowRight size={22} /><div><small>YOUR SPLIT</small><strong>&#8377;162/mo</strong></div><span>75% SAVED</span></div><div className="neo-verified"><ShieldCheck size={18} /><span>VERIFICATION BOT STATUS: <b>SUCCESS</b></span><Check size={17} /></div></div></div></section>

    <section className="neo-trust-strip neo-shell" aria-label="Trusted subscription services"><div className="neo-trust-copy"><span className="neo-card-label">BUILT FOR BETTER SHARING</span><h2>Integrated with<br /><em>the Platforms You Love.</em></h2><p>Join verified subscription groups across the services you already use.</p></div><div className="neo-trust-ticker neo-exact-ticker"><SplitFlapLogoTicker /></div></section>

    <section className="neo-marquee neo-universe" aria-label="Supported services"><div className="neo-marquee-rows">{marqueeRows.map((row, rowIndex) => <div className={`neo-marquee-row neo-marquee-row-${rowIndex % 2 === 0 ? 'left' : 'right'}`} key={rowIndex}><div className="neo-marquee-track">{[...row, ...row].map((service, i) => <span key={`${service}-${i}`}>{service}</span>)}</div></div>)}</div></section>

    <section ref={journeyRef} id="journey" className="neo-engine neo-shell neo-section"><div className="neo-engine-heading"><span className="neo-engine-badge">THE SPLIT ENGINE</span><h2>How SubSplit<br /><em>Works.</em></h2></div><div className="neo-engine-grid"><div className="neo-engine-flow">{[['01 / FIND THE PERFECT PLAN','Explore a live open marketplace of active premium slots. Instantly search and filter by your exact required platform\u2014whether it\u2019s streaming like Netflix, music like Spotify, or productivity suites like ChatGPT Plus.'],['02 / ANALYZE PRICE & REQUIREMENTS','Look before you leap. Deep-dive into the fair-fraction cost split layout, cross-check regional restrictions, and verify active guard-bot compliance checks to see exactly what you save before hitting join.'],['03 / SECURE PAYMENT & AUTOMATED ESCROW','Commit safely. Link a secure monthly UPI AutoPay mandate via compliant Indian gateways. Your funds are held securely in a protected split escrow ledger and are never handed over entirely upfront.'],['04 / JOIN GROUP & CHAT WITH HOST','Unlock immediate sandbox access. Drop directly into an encrypted group hub where you can chat seamlessly with the verified Host to receive profile PINs, configuration details, and active family invitation links.']].map(([title, copy], index) => <article data-step={index} key={title} className={`neo-engine-step ${journeyStep === index ? 'is-active' : ''}`}><span>0{index + 1}</span><h3>{title}</h3><p>{copy}</p></article>)}<aside className="neo-host-callout"><b>HAVE AN ACTIVE MULTI-USER SUBSCRIPTION?</b><p>Toggle Host Mode to instantly create a plan group, list empty screens, and automate your monthly passive revenue route.</p></aside></div><div className="neo-engine-canvas"><AnimatePresence mode="wait"><motion.div key={journeyStep} initial={{opacity:0,scale:.9,y:28}} animate={{opacity:1,scale:1,y:0}} exit={{opacity:0,scale:1.04,y:-18}} transition={{duration:.45,ease:[.22,1,.36,1]}} className="neo-engine-visual"><div className="neo-engine-top"><span>SUBSPLIT / ENGINE LIVE</span><b>STEP 0{journeyStep + 1} / 04</b></div>{journeyStep === 0 && <div className="neo-find-ui"><div className="neo-search">&#x2315; &nbsp; Search Netflix, Spotify, YouTube...</div>{['NETFLIX PREMIUM 4K','SPOTIFY FAMILY','YOUTUBE PREMIUM'].map((service) => <div className="neo-market-row" key={service}><strong>{service}</strong><span>&#x25cf; 2 SLOTS OPEN</span><b>&#8377;162/mo</b></div>)}</div>}{journeyStep === 1 && <div className="neo-ledger-ui"><strong>Official Price: &#8377;649 &#x27a4; Your Fraction: &#8377;162/mo</strong>{['GuardBot Pass Check: SUCCESS','4K HDR Profile Active','0% Shared Risk Confirmed'].map((item) => <span key={item}>&#x2713; {item}</span>)}<b>75% OFF</b></div>}{journeyStep === 2 && <div className="neo-payment-ui"><div><small>UPI AUTOPAY AUTHORIZED</small><strong>&#8377;162.00</strong><span>Receipt #SS-2048 &middot; Monthly mandate active</span></div><p>{['25% WEEKLY ESCROW RELEASES','25% WEEKLY ESCROW RELEASES','25% WEEKLY ESCROW RELEASES','25% WEEKLY ESCROW RELEASES'].map((item, i) => <i key={i}>{item}</i>)}</p></div>}{journeyStep === 3 && <div className="neo-chat-ui"><header><i>H</i><span><b>Verified Host</b><small>Encrypted group hub &middot; online</small></span></header><p className="host-bubble">Hey! Your profile name is Room 3. Here is your login PIN code.</p><p className="user-bubble">Perfect, logged in successfully, thanks!</p><div className="neo-chat-compose">Message host <b>&uarr;</b></div></div>}</motion.div></AnimatePresence></div></div></section>

    <section id="how" className="neo-shell neo-section"><div className="neo-section-heading"><span>01 / THE WORKFLOW</span><h2>SubSplit bridges<br /><em>the gap.</em></h2></div><div className="neo-workflow"><article><div className="neo-card-label">FOR THE HOST</div><h3>Turn unused slots into income.</h3>{hostSteps.map((text, i) => <div className="neo-step" key={text}><b>0{i + 1}</b><span>{text}</span><Check size={15} /></div>)}</article><article className="neo-workflow-alt"><div className="neo-card-label">FOR THE CO-SUBSCRIBER</div><h3>Premium access, without the premium price.</h3>{subSteps.map((text, i) => <div className="neo-step" key={text}><b>0{i + 1}</b><span>{text}</span><Check size={15} /></div>)}</article></div></section>

    <section id="services" className="neo-shell neo-section"><div className="neo-section-heading"><span>02 / THE ENGINE</span><h2>Built for the<br /><em>real world.</em></h2></div><div className="neo-bento"><article><CircleDollarSign size={30} /><h3>Smart group queues.</h3><p>Dynamic matching automatically finds the right number of people for every supported premium app.</p></article><article><ShieldCheck size={30} /><h3>Verification bots.</h3><p>Background checks validate active passwords, invite links, and plan health before charging users.</p></article><article><Wallet size={30} /><h3>UPI AutoPay.</h3><p>Compliant recurring mandates make recurring Indian payments simple, transparent, and safe.</p></article></div></section>

    <section id="trust" className="neo-shell neo-section"><div className="neo-protection"><div><span className="neo-card-label">03 / BUYER PROTECTION</span><h2>Zero-risk escrow<br /><em>protection engine.</em></h2><p>Every rupee follows a disciplined flow. Funds move only when the system confirms that your access is healthy.</p></div><div className="neo-protection-grid"><div><LockKeyhole size={21} /><b>MILESTONE PAYOUTS</b><span className="neo-bars"><i /><i /><i /><i /></span><small>25% released weekly</small></div><div><ShieldCheck size={21} /><b>WEEKLY HEALTH MONITOR</b><span className="neo-check"><Check size={20} /></span><small>Credentials validated</small></div><div><CircleDollarSign size={21} /><b>INSTANT FREEZE SWITCH</b><span className="neo-freeze">FUNDS LOCKED</span><small>Until your issue is resolved</small></div></div></div></section>

    <section id="faq" className="neo-shell neo-section neo-faq"><div className="neo-clarity-intro"><span className="neo-card-label">04 / CLARITY FIRST</span><div className="neo-clarity-stamp">?</div><h2>Questions,<br /><em>answered.</em></h2><p>Clear terms, visible protection, and no hidden surprises before you join a group.</p><div className="neo-clarity-lines"><span>01 / NO GUESSWORK</span><span>02 / FULL VISIBILITY</span><span>03 / FAST SUPPORT</span></div></div><div className="neo-faq-list">{faqs.map(([question, answer], i) => { const isOpen = faq.includes(i); return <div className={`neo-faq-item ${isOpen ? 'is-open' : ''}`} key={question}><button type="button" aria-expanded={isOpen} onClick={() => setFaq((current) => isOpen ? current.filter((item) => item !== i) : [...current, i])}>{question}<ChevronDown size={18} className={isOpen ? 'rotated' : ''} /></button><div className="neo-faq-answer"><p>{answer}</p></div></div> })}</div></section>

    <section className="neo-testimonials neo-section"><div className="neo-shell"><div className="neo-testimonials-head"><span className="neo-card-label">REAL PEOPLE / REAL SAVINGS</span><h2>Clarity from people<br /><em>already inside.</em></h2><p>What members say after finding a better way to share premium access.</p></div><div className="neo-testimonial-marquee"><div className="neo-testimonial-track">{[['A','Aarav Mehta','Netflix group','\u201cFinally, a clean way to split without chasing strangers every month.\u201d'],['S','Sneha Kapoor','Spotify Family','\u201cThe price breakdown made joining feel obvious. No hidden math.\u201d'],['R','Rohan Shah','YouTube Premium','\u201cHost chat gave me confidence before I paid. That detail matters.\u201d'],['N','Nisha Rao','ChatGPT Plus','\u201cJoined in minutes and the invite arrived exactly when promised.\u201d'],['V','Vikram Singh','Netflix group','\u201cGuardBot checks made the whole group feel genuinely verified.\u201d'],['P','Priya Menon','Spotify Family','\u201cI cut my monthly bill and still get the same premium access.\u201d']].map(([initial,name,plan,quote]) => <article className="neo-testimonial-card" key={name}><div className="neo-testimonial-profile"><span>{initial}</span><div><strong>{name}</strong><small>{plan}</small></div><b>VERIFIED</b></div><p>{quote}</p><div className="neo-testimonial-stars">&#x2605;&#x2605;&#x2605;&#x2605;&#x2605;</div></article>)}</div></div></div></section>

    <section id="waitlist" className="neo-cta"><div className="neo-shell"><span>READY TO STOP OVERPAYING?</span><h2>Create your account<br />in under 60 seconds.</h2><div className="neo-cta-actions"><a href="/auth/register" className="neo-button">GET STARTED <ArrowRight size={17} /></a><a href="/auth/login" className="neo-button" style={{background:'#020617',color:'var(--neo-green)',borderColor:'#020617',boxShadow:'5px 5px 0 #fff'}}>ALREADY HAVE AN ACCOUNT? LOGIN <ArrowRight size={17} /></a></div></div></section>

    <footer className="neo-footer neo-shell"><div><a href="#top" className="neo-logo">SUBSPLIT</a><p>Fair access to premium things.</p></div><div><b>PRODUCT</b><a href="#how">How it works</a><a href="#services">Browse slots</a></div><div><b>TRUST</b><a href="#trust">Protection</a><a href="#faq">FAQ</a></div><div><b>LEGAL</b><a href="#waitlist">Terms</a><a href="#waitlist">Privacy</a></div><p className="neo-disclaimer">Brands displayed are for informational reference only and do not imply official partnership.</p></footer>
  </main>
}
